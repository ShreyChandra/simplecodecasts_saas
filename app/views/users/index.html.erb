<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <h1 class="text-center" style="color:white">Dev Box Community</h1>
        <p class="text-center" style="color:white"ra>Welcome to the Dev Box Community. Users must fill out a profile in order to be visible in the community.</p>
        <ul class="list-unstyled">
            <% @users.each do |user| %>
                <% if user.profile %>
                    <li>
                        <div class="row  well <%= cycle('white-bg', '') %>">
                            <div class="col-sm-3 text-center">
                                <% if user.profile.avatar %>
                                    <%= link_to user do %>
                                        <%= image_tag user.profile.avatar.url(:thumb), class: 'round-avatar' %>
                                    <% end %>
                                <% end %>
                            </div>
                            <div class=" col-sm-9 " >
                                <%= link_to user do %>
                                    <h2 class= "index-user-info"><%= user.profile.first_name %> <%= user.profile.last_name %></h2>
                                <% end %>
                                <h4><%= user.profile.job_title %></h4>
                                <% if user.plan_id == 2 %>
                                    <span class="label label-danger">PRO</span>
                                <% end %>
                            </div>
                        </div>
                        
                    </li>
                <% end %>
            <% end %>
        </ul>
        
    </div>
</div>
